# 企业版第一迭代改造计划

## 1. 目标与边界

本迭代只开发 Go 服务端与服务端管理后台，**不修改现有 Electron/Vue 客户端、preload IPC 或本地调用链**。企业版是一个新的产品形态，不兼容当前个人版：不读取、不导入、不迁移个人版的 SQLite、JSON、知识库目录或 Zvec 数据。

服务端只负责以下三类数据维护：

| 数据域      | 第一迭代服务端职责                                 |
| -------- | ----------------------------------------- |
| 企业身份     | 企业、管理员、员工账号、登录会话、角色                       |
| 企业共享模型配置 | 管理员维护模型厂商、模型名、地址和 API Key；登录员工可获取并使用已启用配置 |
| 用户业务数据   | 对话/消息、笔记本/笔记、日程、自动化任务及其运行记录、用户偏好          |

大模型请求、流式响应、Agent 推理、工具调用和自动化实际执行都由桌面客户端直接发起到模型厂商或在本地完成。服务端不代理任何 LLM 请求，不执行 Agent，不保存工具调用过程，也不对调用进行审计；客户端仅将每个用户的调用量等业务数据回传服务端存储和查询。

知识库在企业版第一迭代不可用：不提供知识库、RAG、文件上传、文档解析、嵌入、向量数据库或索引接口。由于本迭代不改客户端，知识库入口的实际隐藏/禁用在客户端接入迭代处理。

## 2. 关键约束：共享 API Key

管理员在服务端配置的模型 API Key 由所有已登录员工客户端使用，而模型请求仍由客户端直接访问厂商。这意味着服务端必须把可用模型配置及其 API Key 下发给客户端；任何能够运行、调试或改造客户端的已授权员工，理论上都可能取得这些共享凭据。

因此，本方案的安全措施是：服务端以 AES-256-GCM 加密静态保存 API Key，响应不写日志、不缓存到浏览器、仅在登录态和组织范围内下发，并支持管理员立即禁用/替换密钥；但这**不能**实现“员工不可见 API Key”。若后续要求保护厂商密钥，必须改变约束，改由服务端代理 LLM 请求或使用厂商支持的可限制、可轮换的临时令牌。

## 3. 服务端设计

### 3.1 技术决策

- 运行时：Go 1.24，HTTP 使用标准库 `net/http` + `chi`。
- 数据库：服务端 SQLite，采用 `modernc.org/sqlite` + `database/sql`；数据库文件位于服务端持久卷，启用 WAL、`foreign_keys=ON` 和连接 busy timeout。SQLite 不作为客户端文件下发。
- 数据访问与迁移：`sqlc` 生成类型安全查询，`goose` 维护版本化 SQL 迁移。仅创建全新的企业版 schema，不包含旧版兼容 SQL 或数据导入工具。
- 认证：邮箱+密码，密码使用 Argon2id；短期 JWT access token 加可撤销、轮换的 refresh token。桌面客户端后续使用系统安全凭据存储 refresh token。
- 数据隔离：业务表均带 `organization_id` 和必要的 `user_id/created_by`；服务端从认证上下文取得身份，所有 repository 查询都必须携带组织范围条件。SQLite 无 RLS，隔离依赖强制的服务层与查询约束，并以跨组织越权测试验收。
- 模型密钥：由部署环境中的主密钥加密保存。主密钥不入库、不入代码、不写入日志；模型配置读取分为“管理员脱敏视图”和“员工直连调用视图”。
- 时间与删除：UTC `datetime` 文本；笔记、会话等用户内容使用 `deleted_at` 软删除，所有查询默认过滤已删除数据。

### 3.2 Go 模块划分

在仓库新增 `server/` 和根 `go.work`。每个客户端业务域对应一个独立 Go 模块，有自己的 `go.mod`、测试和明确依赖方向；第一期可由一个 `api-gateway` 二进制链接部署，不能跨模块直接操作其他模块的表。

| Go 模块                         | 责任                                       | 对应客户端域    |
| ----------------------------- | ---------------------------------------- | --------- |
| `server/api-gateway`          | HTTP 路由、认证中间件、请求校验、统一错误响应、OpenAPI 聚合     | 服务端统一入口   |
| `server/auth-service`         | 企业、员工、角色、登录、令牌、密码修改                      | 新增登录能力    |
| `server/model-service`        | 企业共享模型配置与加密 API Key 管理、客户端模型配置下发         | 设置/模型     |
| `server/conversation-service` | 会话、消息、标题、回滚与历史清理                         | Friday、历史 |
| `server/note-service`         | 笔记本、笔记、笔记搜索                              | 笔记        |
| `server/schedule-service`     | 日程 CRUD 与日期范围查询                          | 日程        |
| `server/automation-service`   | 自动化任务和运行结果记录；不调度、不执行任务                   | 自动化       |
| `server/preferences-service`  | 用户通用偏好和客户端功能开关                           | 设置        |
| `server/admin-web`            | 管理员网页页面与后台会话                             | 新增后台      |
| `server/platform`             | SQLite 连接、配置、加密、认证声明、迁移、日志等共享基础库；不得拥有业务表 | 共享基础设施    |

`agent-service`、`usage-service`、`rag-service` 和向量数据库服务不在本迭代创建。客户端本地 Agent 可以继续使用服务端的会话、笔记、日程等数据 API，但服务端不感知也不记录 Agent 行为。

## 4. 企业版全新数据表

以下是企业版从零创建的服务端 schema，不是对现有个人版 `src-electron/db.js` 表的迁移。

| 表                       | 关键字段                                                                                                                                                                                                                 | 用途                                       |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| `organizations`         | `id`, `name`, `status`, `created_at`                                                                                                                                                                                 | 企业租户                                     |
| `users`                 | `id`, `email`, `display_name`, `password_hash`, `status`, `last_login_at`                                                                                                                                            | 员工账号；邮箱唯一                                |
| `organization_members`  | `organization_id`, `user_id`, `role`, `status`                                                                                                                                                                       | 组织成员与角色，唯一键 `(organization_id, user_id)` |
| `auth_refresh_tokens`   | `id`, `user_id`, `token_hash`, `expires_at`, `revoked_at`, `device_name`                                                                                                                                             | 可撤销登录会话；只保存令牌哈希                          |
| `model_configs`         | `id`, `organization_id`, `provider`, `display_name`, `base_url`, `model_name`, `enabled`, `key_version`                                                                                                              | 管理员维护的共享模型元数据                            |
| `model_credentials`     | `model_config_id`, `ciphertext`, `nonce`, `key_version`, `updated_at`                                                                                                                                                | API Key 密文，与模型元数据分表                      |
| `user_preferences`      | `organization_id`, `user_id`, `language`, `theme`, `system_prompt`, `sidebar_modules`, `updated_at`                                                                                                                  | 可跨设备同步的个人偏好                              |
| `conversations`         | `id`, `organization_id`, `user_id`, `title`, `kind`, `created_at`, `updated_at`, `deleted_at`                                                                                                                        | 用户会话                                     |
| `conversation_messages` | `id`, `organization_id`, `conversation_id`, `role`, `content`, `metadata`, `created_at`                                                                                                                              | 会话消息；`metadata` 为 JSON 文本                |
| `notebooks`             | `id`, `organization_id`, `user_id`, `name`, `description`, `created_at`, `updated_at`, `deleted_at`                                                                                                                  | 用户笔记本                                    |
| `notes`                 | `id`, `organization_id`, `user_id`, `notebook_id`, `title`, `content`, `content_text`, `created_at`, `updated_at`, `deleted_at`                                                                                      | 用户笔记；不含 `knowledge_base_id`              |
| `schedule_events`       | `id`, `organization_id`, `user_id`, `title`, `start_date`, `end_date`, `start_time`, `end_time`, `all_day`, `description`, `color`, `reminder`, `completed`, `priority`, `created_at`, `updated_at`, `deleted_at`    | 用户日程                                     |
| `automation_tasks`      | `id`, `organization_id`, `user_id`, `name`, `instruction`, `model_config_id`, `trigger_type`, `trigger_config`, `cron_expression`, `enabled`, `last_run_at`, `next_run_at`, `created_at`, `updated_at`, `deleted_at` | 客户端本地执行的任务配置                             |
| `automation_runs`       | `id`, `organization_id`, `user_id`, `task_id`, `conversation_id`, `trigger`, `status`, `started_at`, `completed_at`, `duration_ms`, `output`, `error`                                                                | 客户端回传的运行结果                               |
| `llm_usage_records`     | `id`, `organization_id`, `user_id`, `model_config_id`, `model_name`, `provider`, `prompt_tokens`, `completion_tokens`, `total_tokens`, `reasoning_tokens`, `source`, `occurred_at` | 客户端回传的模型调用量数据 |

必须建立以 `organization_id, user_id` 为前缀的业务查询索引；会话消息索引 `(organization_id, conversation_id, created_at)`；日程索引 `(organization_id, user_id, start_date, end_date)`；模型配置索引 `(organization_id, enabled)`。SQLite FTS5 用于同一用户范围内的笔记全文检索，触发器只维护笔记索引。

明确不创建、不迁移的个人版数据：`agent_threads`、`agent_memories`、`agent_tool_logs`、`file_status`、`parent_docs`、Zvec collection、`usage.json`、本地技能/MCP/Python/备份/分享配置和任何旧版数据导入记录。

## 5. 第一迭代 API

所有接口位于 `/api/v1`，采用 JSON，统一返回 `{ "data": ..., "requestId": "..." }`。除登录、刷新令牌和健康检查外均需认证；资源查询只能在当前组织及当前用户数据范围内进行。管理端接口额外要求 `org_admin` 或 `org_owner`。

| 域       | 接口                                                                                                                                                                         | 说明                                              |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 认证      | `POST /auth/login`、`POST /auth/refresh`、`POST /auth/logout`、`GET /me`、`POST /auth/change-password`                                                                         | 员工登录与会话管理                                       |
| 成员      | `GET /admin/members`、`POST /admin/members`、`PATCH /admin/members/{id}`、`POST /admin/members/{id}/reset-password`                                                           | 管理员创建、启停、改角色、重置密码                               |
| 企业模型管理  | `GET/POST /admin/model-configs`、`GET/PATCH/DELETE /admin/model-configs/{id}`                                                                                               | 管理员维护厂商、地址、模型、启用状态和 API Key；读取时仅展示掩码            |
| 客户端模型配置 | `GET /model-configs/active`                                                                                                                                                | 已登录员工获取可直连调用的已启用模型配置与运行时 API Key；响应禁止被网关/应用日志记录 |
| 偏好      | `GET/PATCH /preferences`                                                                                                                                                   | 用户个人设置；不包括本地 Python、MCP、目录等设备设置                 |
| 对话      | `GET/POST /conversations`、`GET/PATCH/DELETE /conversations/{id}`、`GET/POST /conversations/{id}/messages`、`POST /conversations/{id}/rollback`、`POST /conversations/cleanup` | 用户自己的对话数据                                       |
| 笔记      | `GET/POST /notebooks`、`GET/PATCH/DELETE /notebooks/{id}`、`GET/POST /notes`、`GET/PATCH/DELETE /notes/{id}`、`GET /notes/search`                                              | 用户自己的笔记数据                                       |
| 日程      | `GET/POST /schedule-events`、`GET/PATCH/DELETE /schedule-events/{id}`                                                                                                       | 列表支持 `startDate`、`endDate`                      |
| 自动化数据   | `GET/POST /automation/tasks`、`GET/PATCH/DELETE /automation/tasks/{id}`、`GET/POST /automation/runs`、`GET/DELETE /automation/runs/{id}`                                      | 只读写配置和结果；服务端不触发执行                               |
| 调用量数据   | `GET/POST /usage-records`、`GET /usage-records/summary` | 客户端回传调用量；服务端只存储和聚合，不调用或代理模型 |
| 平台      | `GET /healthz`、`GET /readyz`、`GET /openapi.json`                                                                                                                           | 部署和联调                                           |

不提供的接口：LLM 对话/补全/流式代理、Agent、工具审批、工具日志、用量统计、知识库、RAG、向量检索、向量写入、文件上传、模型余额查询、MCP、Python、文件系统、技能、备份和分享服务。

## 6. 认证、数据隔离与模型配置流程

1. 部署命令创建首个 `organization` 及 `org_owner` 管理员。管理员在后台创建员工账号和共享模型配置。
2. 员工用邮箱和密码登录；服务端校验账号与成员状态，签发 access token 和 refresh token。
3. API 网关验证令牌并将 `organization_id`、`user_id`、`role` 写入 `context.Context`。业务服务只从该上下文取得数据归属。
4. 用户数据接口固定使用当前 `organization_id + user_id`，管理员也不能通过常规业务 API 读取员工的对话、笔记或日程内容。
5. 已登录客户端调用 `GET /model-configs/active`，取得管理员启用的模型配置后，**直接请求模型厂商**。服务端不位于调用路径中，也不接收模型响应或 token 用量。
6. 管理员禁用或更新模型配置后，客户端在下一次配置刷新时停止使用旧配置；客户端接入迭代需实现短周期刷新、失效处理及运行内存清除。

## 7. 后台管理网页

后台由 `server/admin-web` 提供 Go HTML 模板和少量原生 JavaScript/CSS，不改现有桌面客户端，也不增加独立前端工程。后台 Cookie 使用 `HttpOnly`、`Secure`、`SameSite` 属性，页面操作必须带 CSRF 防护。

首期页面：

- `/admin/login`：管理员登录、登出、首次改密。
- `/admin`：企业概览，仅显示成员数、启用模型数和账号状态统计。
- `/admin/members`：成员列表、创建账号、启停账号、角色调整、重置密码。
- `/admin/model-configs`：共享模型新增、编辑、启停、替换 API Key；页面只显示掩码后的 Key。
- `/admin/organization`：企业名称及基础信息维护。

后台不提供模型调用监控、Agent 监控、工具审计、员工业务正文查看或个人版数据导入页面。

## 8. 实施顺序与验收

1. 创建 `go.work`、各 Go 模块骨架、配置、SQLite 连接池、WAL 设置、迁移框架、OpenAPI 骨架和 CI（`gofmt`、`go vet`、lint、测试）。
2. 建立全新的 SQLite schema、索引、SQLite FTS5 笔记索引和首个管理员初始化命令。
3. 实现认证、组织成员和 RBAC；完成令牌轮换、账号停用、跨组织/跨用户越权测试。
4. 实现共享模型配置加密保存、管理员管理 API 与员工运行时配置下发 API；验证日志和后台页面均不泄露 API Key。
5. 实现对话、笔记、日程、自动化数据和偏好服务及契约测试。
6. 实现管理后台网页与端到端权限测试。
7. 输出部署说明、SQLite 备份/恢复说明、环境变量清单、主密钥轮换说明、OpenAPI 文档和下一迭代客户端接入清单。

验收标准：

- 企业版以空白 SQLite 数据库启动，不读取或导入任何个人版数据。
- 未认证请求无法访问业务数据；不同企业、不同员工之间无法通过猜测资源 ID 读取或修改对方数据。
- 管理员可管理员工及共享模型；员工可取得已启用模型配置，并由客户端直接访问模型厂商。
- 模型 API Key 只以密文落库，管理页面、常规日志和管理员查询 API 均不返回明文；方案文档和产品说明明确员工客户端直连导致的凭据可见风险。
- 对话、笔记、日程、自动化任务/结果和偏好均可按当前登录用户完成 CRUD；服务端不产生 Agent、工具、LLM 请求或用量记录。
- `/knowledge`、`/rag` 和任何向量数据库接口均不存在；当前客户端不因本迭代服务端代码而改动。
- 所有 Go 模块通过格式化、静态检查、单元测试、SQLite 集成测试及迁移升级测试。

## 9. 暂不实施项

- Electron/Vue 客户端登录页、令牌安全存储、HTTP 服务适配、现有 IPC 替换、服务端数据读写切换、离线策略和冲突处理。
- 客户端知识库入口的隐藏/禁用，及所有 RAG、文件、对象存储、Embedding、向量数据库与索引能力。
- 服务端代理模型请求、流式 SSE/WebSocket、模型调用日志、余额查询、配额和计费。
- 服务端 Agent、自动化调度/执行、工具调用、工具审批、记忆、审计和沙箱能力。
- 个人版 SQLite/JSON/知识库数据的兼容、迁移、导入或回滚。
- SSO、MFA、邮件邀请、找回密码、LDAP、跨组织用户、数据保留和灾备演练。

## 10. 待确认默认项

1. 服务端数据库确定为单实例 SQLite；首期部署需要一个持久化磁盘卷，且不支持多实例同时写同一数据库文件。未来如需高并发或多副本部署，再迁移至 PostgreSQL。
2. 企业采用“一个 organization 对应一家公司”，角色为 `org_owner`、`org_admin`、`member`，首期用户名为邮箱+密码。
3. 管理员配置的厂商 API Key 将实际下发给已登录员工客户端，以满足客户端直连模型厂商；接受该密钥可被员工客户端环境提取的事实。
4. 第一迭代只交付服务端和后台，客户端维持不变；下一迭代才接入认证、数据 API、模型配置刷新，并禁用知识库模块。
